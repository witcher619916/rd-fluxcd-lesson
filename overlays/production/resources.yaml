apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
spec:
  template:
    spec:
      containers:
      - name: course-app
        resources:
          requests:
            memory: "200Mi"
            cpu: "250m"
          limits:
            memory: "350Mi"
            cpu: "500m"

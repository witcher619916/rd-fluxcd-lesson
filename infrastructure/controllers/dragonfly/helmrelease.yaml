apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dragonfly-operator
  namespace: dragonfly-operator-system
spec:
  chartRef:
    kind: OCIRepository
    name: dragonfly-operator
    namespace: dragonfly-operator-system
  interval: 10m
